# Secure-by-default npm configuration
# Prevent malicious postinstall scripts from executing automatically
ignore-scripts=true

# Block untrusted git-based URL dependencies
allow-git=none

# Enforce explicit engine declarations
engine-strict=true
